# /sign command – Placeholder for Reown or MPC signing
async def sign(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("🖋 Signer action detected! We'll integrate MPC (e.g. Reown) here soon.")
