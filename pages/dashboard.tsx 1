// pages/dashboard.tsx
import React from "react";
import SafeTable from "@/components/SafeTable";
import { loadSafeConfigs } from "@/utils/loadSafes";

const DashboardPage = () => {
  const safesByChain = loadSafeConfigs();

  return (
    <div>
      <h1>Safe Dashboard</h1>
      <SafeTable safesByChain={safesByChain} />
    </div>
  );
};

export default DashboardPage;
