name: 🛡️ Safe Status on PR

on:
  pull_request:
    types: [opened]

jobs:
  comment-safe-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install node-fetch@2

      - name: Comment Safe Status
        env:
          GITHUB_TOKEN: ${{ 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 }}
          SAFE_ADDRESS: 0xAfD5f60aA8eb4F488eAA0eF98c1C5B0645D9A0A0
        run: node scripts/commentSafeStatus.js
