useEffect(() => {
  const interval = setInterval(() => {
    fetchPendingTransactions(safeAddress, chainId).then(setTxs);
  }, 30000);

  return () => clearInterval(interval);
}, [safeAddress, chainId]);
